const Sum = (num1, num2) => {
    console.log(`${num1} + ${num2} = ${num1 + num2}`);
};

Sum(12, 12);

const Subtract = (num1, num2) => {
    console.log(`${num1} - ${num2} = ${num1 - num2}`);
};

Subtract(12, 12);